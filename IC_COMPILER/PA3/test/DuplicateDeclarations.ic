class A {}
class A {  // Should be an error
  static void main(string[] args1) {}
  static void main(int a, int a) {    // Should be two errors
    int a;     // Should be an error
    int b;
    string b;     // Should be an error
    int c;
    {
      int c;     // Should NOT be an error
      int d;
      int d;     // Should be an error
    }
  }
  int a;
  void a() {}
  void a() {}
  static void a() {}
}